# Canvas LMS Configuration
CANVAS_BASE_URL=https://your-institution.instructure.com
CANVAS_ACCESS_TOKEN=your_canvas_access_token_here

# HTTP Server Configuration (for Open WebUI and n8n integration)
# Note: Open WebUI typically runs on port 3000, so we use 3001 to avoid conflicts
PORT=3001
HOST=0.0.0.0

# Security Configuration (REQUIRED for production deployments!)
# Generate a secure key with: openssl rand -hex 32
# Leave empty for local development (authentication will be disabled)
API_KEY=

# Optional: Environment and Logging
NODE_ENV=development
LOG_LEVEL=info